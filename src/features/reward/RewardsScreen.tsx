import HomeScreen from '../../components/HomeScreen';

const RewardsScreen: React.FC = () => {
    return (
        <HomeScreen id="rewards-screen" title="Rewards" />
    );
};

export default RewardsScreen;
